# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x
    - name: Restore dependencies
      run:  dotnet restore ./backend/src/RambleOn.Api/RambleOn.Api.csproj
    - name: Build
      run: dotnet build ./backend/src/RambleOn.Api/RambleOn.Api.csproj --no-restore
    - name: Test
      run: dotnet test ./backend/src/RambleOn.Api/RambleOn.Api.csproj --no-build --verbosity normal
